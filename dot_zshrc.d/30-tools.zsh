# --- mcfly: inteligentne szukanie historii komend ---
# export MCFLY_RESULTS=50
# export MCFLY_FUZZY=2
# export MCFLY_KEY_SCHEME=vim
# eval "$(mcfly init zsh)"

eval "$(atuin init zsh)"

# --- zoxide: smart cd ---
eval "$(zoxide init zsh)"
